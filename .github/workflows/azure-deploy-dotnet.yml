name: Build and Deploy Demo App (.NET)

on:
  push:
    branches:
      - main

env:
  APP_NAME: dummy-app

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '6.0.x'

      - name: Simulate Build
        run: echo "No .NET project found — skipping build"

      - name: Simulate Azure Deployment
        run: echo "✅ Simulated deploy to Azure Web App: $APP_NAME (no real credentials)"

